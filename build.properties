# Директории проекта
src.dir=app/src/main/java
web.dir=app/src/main/webapp
build.dir=app/build
classes.dir=${build.dir}/classes/java/main
lib.dir=${build.dir}/libs
dist.dir=${build.dir}/dist
tmp.dir=${build.dir}/tmp

# JAR параметры
jar.name=app.jar
main.class=org.itmo.controller.MakePoint
manifest.file=${tmp.dir}/war/MANIFEST.MF
version=1.0.0

# Тестирование
test.src.dir=app/src/test/java
test.classes.dir=${build.dir}/classes/java/test
test.report.dir=${build.dir}/test-reports

# Локализация
resources.dir=app/src/main/resources
n2a.src=resources
n2a.dest=resources/ascii

# SCP параметры
scp.host=example.com
scp.user=user
scp.password=pass
scp.remoteDir=/path/to/remote

# Replace regex для alt
replace.pattern.old=org\.itmo
replace.pattern.new=org\.itmo\.alt

# Другие настройки
javadoc.dir=${build.dir}/javadoc
git.repo=file:///path/to/repo